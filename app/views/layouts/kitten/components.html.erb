<% Kitten.configuration.default_categories.each do |category| %>
  <%= render_category(category) %>
  <% category[:components].each do |component| %>
    <%= render_component(component[:path],
                         examples_display: component[:examples_display]) %>
  <% end %>
<% end %>
