<% Kitten.configuration.component_categories.each do |category| %>
  <%= render_category(category) %>
  <% category[:components].each do |component| %>
    <%= render_component(component[:path], title: component[:name]) %>
  <% end %>
<% end %>
