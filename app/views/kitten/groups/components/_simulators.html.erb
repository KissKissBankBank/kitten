<%= render_component 'components/simulators/loan-simulator',
      title: 'Loan simulator',
      description: 'This simulator is used when the user needs to make a loan
      decision based on several parameters. It is important to differentiate
      what the user wants to choose (the data he enters) from what the user
      needs to know (the result).',
      examples_display: :vertical %>

<%= render_component 'components/simulators/simulator-result',
      title: 'Simulators result',
      description: 'Simulator results is used to display data. Data changes with
      the user’s actions. Cf. “Simulators” Molecules for more details on the
      usage.' %>
