{
  "name": "kitten-example-dummy",
  "version": "1.0.0",
  "license": "MIT",
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ],
  "scripts": {
    "postinstall": "cd client && yarn install",
    "build:dev": "(cd client && yarn build:dev)",
    "build": "(cd client && yarn build --silent)",
    "hot-assets": "(cd client && yarn hot-assets)"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "kitten-launcher": "^1.0.0",
    "webpack": "^2.5.1"
  }
}
